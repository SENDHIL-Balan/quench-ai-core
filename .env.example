# Quench AI — environment configuration (placeholders only, never commit real keys)

# Google Gemini API key. Used server-side only.
# Get one at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Optional: managed AI gateway key (used automatically when GEMINI_API_KEY is absent).
# LOVABLE_API_KEY=managed-by-the-platform
